{
  "config": {
    "step": {
      "user": {
        "title": "Security Assistant — Setup",
        "description": "Enter the NetFlow listener address and your internal network ranges. All advanced settings can be configured from the Security Assistant panel after setup.",
        "data": {
          "bind_host": "NetFlow bind host (e.g. 0.0.0.0)",
          "bind_port": "NetFlow bind port (UDP, default 2055)",
          "enable_netflow_listener": "Enable NetFlow listener",
          "internal_networks": "Internal networks (comma-separated CIDRs)"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Security Assistant — Listener Settings",
        "description": "Update the NetFlow listener and internal networks. All advanced settings are available from the Security Assistant panel.",
        "data": {
          "bind_host": "NetFlow bind host",
          "bind_port": "NetFlow bind port (UDP)",
          "enable_netflow_listener": "Enable NetFlow listener",
          "internal_networks": "Internal networks (comma-separated CIDRs)"
        }
      }
    }
  }
}
